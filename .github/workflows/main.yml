name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: 5efb72b3-0b6a-4684-92b9-273c4b32bca8
          heroku_app_name: "testfiletolinl" #Must be unique in Heroku
          heroku_email: "testdeploy@protonmail.com"
      #    usedocker: true
